# 🚀 دليل التشغيل السريع | Quick Start Guide

## نظام إبراهيم للمحاسبة
## Ibrahim Accounting System

---

## ⚡ التشغيل السريع في 5 دقائق

### الخطوة 1: تنفيذ قاعدة البيانات

1. **اذهب إلى Supabase SQL Editor:**
   - رابط المشروع: https://supabase.com/dashboard/project/kyxbhmvxtudrvdbhpjbz/sql
   
2. **نفذ Migration Script:**
   - افتح ملف: `database/migrations/001_initial_schema.sql`
   - انسخ كل المحتوى
   - الصقه في SQL Editor
   - اضغط ▶ Run

3. **نفذ Seed Script:**
   - افتح ملف: `database/seeds/001_initial_data.sql`
   - انسخ كل المحتوى
   - الصقه في SQL Editor
   - اضغط ▶ Run

### الخطوة 2: احصل على Service Role Key

1. اذهب إلى: https://supabase.com/dashboard/project/kyxbhmvxtudrvdbhpjbz/settings/api
2. انسخ `service_role` key (تحت "Service role")
3. افتح ملف `backend/.env`
4. استبدل `your_service_role_key_from_supabase_dashboard` بالـ key الذي نسخته

### الخطوة 3: تشغيل Backend

```bash
# افتح PowerShell في مجلد المشروع
cd backend

# ثبت المكتبات
npm install

# شغل الخادم
npm run dev
```

يجب أن ترى:
```
🚀 Server is running on port 5000
📦 Environment: development
```

### الخطوة 4: تشغيل Frontend

```bash
# افتح PowerShell جديد في مجلد المشروع
cd frontend

# ثبت المكتبات
npm install

# شغل التطبيق
npm run dev
```

يجب أن ترى:
```
VITE ready in XXX ms
➜  Local:   http://localhost:5173/
```

### الخطوة 5: افتح التطبيق

1. افتح المتصفح
2. اذهب إلى: http://localhost:5173
3. اضغط "Register" لإنشاء حساب جديد
4. املأ البيانات وأنشئ متجرك!

---

## 📋 متطلبات النظام

- ✅ Windows 10/11 (أنت على Windows 22H2 ✓)
- ✅ Node.js 18+ (قم بتثبيته من https://nodejs.org)
- ✅ اتصال بالإنترنت

---

## 🔑 بيانات الاتصال بقاعدة البيانات

**تم إعدادها مسبقاً في الملفات:**

Backend `.env`:
```env
SUPABASE_URL=https://kyxbhmvxtudrvdbhpjbz.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJI...
SUPABASE_SERVICE_ROLE_KEY=<احصل عليه من الخطوة 2>
```

Frontend `.env`:
```env
VITE_API_URL=http://localhost:5000/api
VITE_SUPABASE_URL=https://kyxbhmvxtudrvdbhpjbz.supabase.co
```

---

## 🎯 التحقق من التثبيت

### اختبار Backend:
افتح في المتصفح: http://localhost:5000/health

يجب أن ترى:
```json
{
  "success": true,
  "data": {
    "status": "healthy",
    ...
  }
}
```

### اختبار Frontend:
يجب أن تفتح صفحة Login بدون أخطاء.

---

## ❌ حل المشاكل الشائعة

### مشكلة: "npm not found"
**الحل:** ثبت Node.js من https://nodejs.org

### مشكلة: "Port 5000 already in use"
**الحل:** 
```bash
# أوقف العملية المستخدمة للمنفذ أو غير المنفذ في backend/.env
PORT=5001
```

### مشكلة: "ECONNREFUSED" أو "Network Error"
**الحل:**
1. تأكد من تشغيل Backend (يجب أن يعمل على port 5000)
2. تأكد من `VITE_API_URL` في frontend/.env يشير إلى `http://localhost:5000/api`

### مشكلة: أخطاء في SQL
**الحل:**
- تأكد من نسخ السكريبت كاملاً
- نفذ السكريبتات بالترتيب (001_initial_schema.sql أولاً)

### مشكلة: "Invalid API key"
**الحل:**
- تأكد من نسخ `service_role` key بشكل صحيح
- تأكد من عدم وجود مسافات قبل أو بعد الـ key

---

## 📱 معلومات الاتصال والدعم

**للدعم الفني:**
- 📧 Email: systemibrahem@gmail.com
- 📱 Phone: +963 994 054 027
- 💬 WhatsApp: +963 994 054 027

**خطط الاشتراك:**
- 🆓 تجربة مجانية: 30 يوم
- 💵 شهري: $5
- 💰 6 أشهر: $30
- 🎁 سنوي: $40 (وفّر $20!)

---

## 🎨 المميزات الرئيسية

### ✅ تم تنفيذها:
- ✓ نظام تسجيل الدخول والمصادقة
- ✓ نظام متعدد المتاجر
- ✓ دعم 3 عملات (TRY, SYP, USD)
- ✓ دعم 3 لغات (عربي، إنجليزي، تركي)
- ✓ الوضع الليلي
- ✓ نظام الصلاحيات
- ✓ نظام الاشتراكات
- ✓ قاعدة بيانات كاملة مع جميع الجداول

### 🔄 قيد التطوير:
- [ ] واجهات CRUD كاملة للواردات/الصادرات
- [ ] إدارة المستودع الكاملة مع التنبيهات
- [ ] نظام الرواتب مع الحسابات التلقائية
- [ ] التقارير مع PDF/Excel
- [ ] المخططات التفاعلية
- [ ] تطبيق Android

---

## 📂 هيكل المشروع

```
ibrahem-system/
├── backend/                 # Node.js + Express API
│   ├── src/
│   │   ├── config/         # إعدادات
│   │   ├── controllers/    # Controllers
│   │   ├── routes/         # Routes
│   │   ├── middleware/     # Middleware
│   │   └── server.js       # نقطة البداية
│   └── .env                # متغيرات البيئة
│
├── frontend/               # React + Vite
│   ├── src/
│   │   ├── components/    # مكونات واجهة المستخدم
│   │   ├── pages/         # الصفحات
│   │   ├── store/         # Redux Store
│   │   └── App.jsx        # المكون الرئيسي
│   └── .env               # متغيرات البيئة
│
├── database/              # قاعدة البيانات
│   ├── migrations/        # Migration Scripts
│   └── seeds/             # Seed Data
│
├── README.md              # معلومات عامة
├── SETUP.md              # دليل الإعداد التفصيلي
└── QUICKSTART.md         # هذا الملف
```

---

## 🚦 الخطوات التالية

بعد التشغيل الناجح:

1. **سجل حساب جديد** من صفحة Register
2. **استكشف لوحة التحكم**
3. **جرب تغيير اللغة** (عربي/إنجليزي/تركي)
4. **جرب الوضع الليلي**
5. **ابدأ إضافة البيانات**

---

## 📚 موارد إضافية

- **Supabase Docs:** https://supabase.com/docs
- **React Docs:** https://react.dev
- **Tailwind CSS:** https://tailwindcss.com

---

## 🔒 الأمان

النظام يتضمن:
- ✓ تشفير كلمات المرور (Bcrypt)
- ✓ JWT Authentication
- ✓ Refresh Tokens
- ✓ Rate Limiting
- ✓ CORS Protection
- ✓ Input Validation
- ✓ Row Level Security (RLS)

---

## 📄 الترخيص

جميع الحقوق محفوظة © 2025 نظام إبراهيم للمحاسبة

---

**🎉 مبروك! أنت الآن جاهز لاستخدام نظام إبراهيم للمحاسبة!**

للأسئلة والدعم، تواصل معنا عبر:
📧 systemibrahem@gmail.com
💬 WhatsApp: +963 994 054 027
